/**
 *  Constraint
 *
 *  Constraints are built by Models. A constraint represents a residual, usually gaussian, that
 *  has a mean of zero and has a variance that tunes the residual's influence on the cost function.
 *
 *  Filter will use the constraints to generate a problem. 
 *  
 **/